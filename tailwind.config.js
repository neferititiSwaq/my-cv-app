/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,jsx,ts,tsx}', // Add paths to your source files (React components)
    './public/index.html',         // Add the path to your index.html file (if needed)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
